'use strict';
 
/*var hm = angular.module('hm');

hm.factory('Reward', function($resource) {
    return $resource('/rewards/:id', {id: '@id'}, 
                      { 'create': { method: 'POST' }});
});
 
/*app.factory('Comment', function($resource) {
    return $resource('/forums/:forumId/comments/:id', {forumId: '@forumId', id: '@id'});
});


//believe this is old version
/*angular.service('Photos', function($resource) {
 return $resource('photographers/:photographer_id/galleries/:gallery_id/photos', {},
                  { 'index': { method: 'GET', isArray: true }});
});

angular.service('SelectedPhotos', function($resource) {
 return $resource('selected_photos/:selected_photo_id', {},
                    { 'create': { method: 'POST' },
                      'index': { method: 'GET', isArray: true },
                      'update': { method: 'PUT' },
                      'destroy': { method: 'DELETE' }});
});
*/

/*angular.service('Rewards', function($resource) {
  return $resource('rewards/:reward_id', {},
                    { 'create': { method: 'POST' }});
});

var HMModule = angular.module('HMModule', []);

var myModule = angular.module('myModule', []);
 
// register a new service
myModule.value('appName', 'MyCoolApp');
 
// configure existing services inside initialization blocks.
myModule.config(function($locationProvider) {
  // Configure existing providers
  $locationProvider.hashPrefix('!');
});*/